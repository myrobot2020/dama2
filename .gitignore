creds/
rag_index/
__pycache__/
*.py[cod]
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
.vscode/
.idea/
.coverage
htmlcov/
.DS_Store
Thumbs.db
.python-version

# Per-sutta chat history from AN1 reading UI (local disk)
sutta_chat_history/
global_chat_history/
sutta_notes/

# Local / generated outputs
output/
*.log
*.sqlite
*.db

# Avoid stray root-level AN JSON artifacts (canonical data lives under processed transcript/)
an*.json
# Exception: AN1 source for local RAG + Dockerfile.an1 / CI (path contains a space).
!processed scipts2/an1.json
!processed scipts2/an2.json
.env
_subtitle_tmp/

# Only the first sample transcript (001_*) is committed.
# All others are downloaded via download_transcripts.py
0[0-9][2-9]_*.txt
0[1-9][0-9]_*.txt

# Cursor / local IDE
.cursor/

# Local Chroma persist (dev); Cloud Run uses Vertex + bundled JSON
rag_index_an1/
rag_index_an2/

# Fine-tune output dirs (large checkpoints)
finetune/out-*/

# Large raw Grok export (not needed in repo; regenerate locally if required)
grok-export/prod-grok-backend.json

# Scratch / duplicate JSON next to canonical an1.json + an2.json
processed scipts2/Untitled
processed scipts2/js.txt
processed scipts2/* copy*.json
